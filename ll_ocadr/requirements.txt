# Core dependencies
torch>=2.0.0
transformers>=4.35.0
vllm>=0.2.0

# 3D geometry processing
torch-cluster>=1.6.0  # fused CUDA FPS kernel for GeometryNet; optional but strongly recommended
trimesh>=3.23.0

open3d>=0.17.0
numpy>=1.24.0
scipy>=1.10.0

# Optional pre-trained models
huggingface-hub>=0.19.0

# Utilities
tqdm>=4.65.0
pillow>=10.0.0
matplotlib>=3.7.0
